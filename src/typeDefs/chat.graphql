type Query {
    getChatByUser(userId: String): [Chat]
    getChatByMembers(members: [String]): Chat
}

type Mutation {
    createChat(members: [String]): Chat
    updateChat(chatId: String): Boolean
}
type Chat {
  _id: ID
  members: [String]
  isBlock: Boolean
  lastActivity: Float
}