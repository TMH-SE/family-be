type Query {
    getChatByUser(userId: String): [Chat]
    getChatByMembers(members: [String]): Chat
}

type Mutation {
    createChat(members: [String]): Chat
    deleteAll: Boolean
}
type Chat {
  _id: ID
  members: [String]
  isBlock: Boolean
}